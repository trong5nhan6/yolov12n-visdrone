# IAWR — Input-Adaptive Width Routing
model: yolov12n
idea: iawr
pretrained: yolo12n.pt
epochs: 200          # OFA training cần nhiều epoch hơn
batch_size: 16
name: yolov12n_iawr
# IAWR-specific
iawr_width_options: [0.5, 0.75, 1.0]
iawr_router_img_size: 128
iawr_router_backbone: mobilenetv3_small
