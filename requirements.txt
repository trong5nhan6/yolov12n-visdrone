# =============================================================================
# YOLOv12n VisDrone — Dependencies
# pip install -r requirements.txt
# =============================================================================

# Core
torch>=2.1.0
torchvision>=0.16.0
ultralytics>=8.3.0      # YOLOv12 support có từ >= 8.3

# Config & Utils
pyyaml>=6.0
omegaconf>=2.3.0        # nested config merge tiện hơn yaml thuần
rich>=13.0.0            # đẹp hơn khi print progress

# Image & Data
opencv-python>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0

# Metrics
pycocotools>=2.0.7      # COCO-style mAP

# Logging (tuỳ chọn — bỏ comment nếu dùng)
# wandb>=0.17.0
# tensorboard>=2.14.0

# Dev tools
tqdm>=4.66.0
